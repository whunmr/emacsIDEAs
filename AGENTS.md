## Test Data Rule

- Do not copy user-provided real project paths, domains, package names, symbol names, or other example-specific strings into tests.
- In tests, always use dummy or semantically neutral literals such as `/tmp/x.go`, `example.com/project/pkg`, `WidgetService`, or similar placeholders.
